providers = ["python"]

[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "gcc"]

[phases.install]
cmds = ["pip install --no-cache-dir -r backend/requirements-prod.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
