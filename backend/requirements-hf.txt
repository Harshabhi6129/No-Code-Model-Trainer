# HuggingFace Spaces — production requirements
# torch is intentionally excluded here and installed separately in the Dockerfile
# using the CPU-only wheel (--index-url https://download.pytorch.org/whl/cpu)
# to keep the image ~250 MB instead of 2.5 GB for the CUDA build.

fastapi==0.104.1
uvicorn[standard]==0.24.0
pandas==2.1.3
python-multipart==0.0.6
websockets==12.0
python-dotenv==1.0.0
pydantic>=2.0.0
requests>=2.31.0
httpx>=0.25.0

# ML stack (CPU mode — torch already installed before this file is processed)
transformers>=4.35.0
datasets>=2.14.0
scipy>=1.10.0
scikit-learn>=1.3.0
accelerate>=0.26.0
peft>=0.7.0
optimum>=1.16.0

# Hyperparameter optimisation
optuna>=3.6.0

# Export formats
onnx>=1.14.0
onnxscript>=0.1.0

# Platform integrations
anthropic>=0.20.0
huggingface_hub>=0.20.0
supabase>=2.0.0
